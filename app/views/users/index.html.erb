<style type="text/css">
<!--
table {
  /*
  background: #dfdef9;
  background: #0000ff;
  background: #cfd0e0;
  */
  width : 106%;
  table-layout: fixed;
  margin-left: -20px;
  margin-bottom: 20px;
}
/*
tr td:first-child {
  padding-left: 10px;
}
*/
td {
/*    display: block; */
  font-size: 100%;
  width: 35%;
  overflow: hidden;
  float: left;
  padding: 5px 20px 00px 00px;
  margin-left: 40px;
  <% if current_user.admin? %>
    padding: 5px 7px 00px 00px;
    margin-left: 20px;
    width: 25%;
  <% end %>
  /*background: #ffffbf;*/
}
td:last-child {
  margin-right: 0;
}
-->
</style>
<%= will_paginate %>
<table class="round" summary="User list">
  <tr>
    <th>
      <%= "#{@users.count} users" %>
    </th>
    <% @users.each do |u| %>
      <!--<tr class="sidebar round">-->
    <tr>
      <td><%= u.email_addr[/[^@]+/] %></td>
      <td><%= link_to u.email_addr, u %></td>
      <% if current_user.admin? %>
        <td>
        <%= link_to 'delete', u, :method => :delete,
          :confirm => "Confirm delete of #{u.email_addr}",
          :title => "Delete #{u.email_addr}" %>
        </td>
      <% end %>
    </tr>
    <% end %>
  </tr>
</table>
<%= will_paginate %>
