<% if ! object.nil? && object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      Update failed due to <%= pluralize(object.errors.count, 'error') %>:
    </div>
    <ul>
    <% object.errors.full_messages.each do |m| %>
      <li><%= m %></li>
    <% end %>
    </ul>
  </div>
<% end %>
